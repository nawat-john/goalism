services:
  db:
    image: postgres:16
    container_name: studyplanner-db
    environment:
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: studyplanner
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dev -d studyplanner"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata: {}
