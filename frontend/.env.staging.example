# Staging — see backend/.env.staging.example. No staging infra provisioned yet.
NEXT_PUBLIC_API_URL="https://staging-api.studyplanner.example/api/v1"

# Optional — leave unset to disable error reporting.
NEXT_PUBLIC_SENTRY_DSN=""
