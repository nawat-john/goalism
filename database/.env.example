# PostgreSQL connection used by Prisma for migrations, generate, and seed.
# Matches the local docker-compose Postgres (see repo root docker-compose.yml).
DATABASE_URL="postgresql://dev:dev@localhost:5432/studyplanner?schema=public"
