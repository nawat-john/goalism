# Staging — see backend/.env.staging.example. Used when running
# `pnpm migrate:deploy`/`pnpm seed` by hand against the staging DB.
DATABASE_URL="postgresql://<staging-user>:<staging-pass>@<staging-host>:5432/studyplanner_staging?schema=public"
